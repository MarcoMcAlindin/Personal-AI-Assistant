# SENDOFF: VOS-015/017/018 — Frontend Logic Integration

- **Agent:** Mr. Blue (Frontend Expert)
- **Phase:** 4 (Logic & Automation)
- **Status:** **UNBLOCKED**

## Mission Specs
Your objective is to move the remaining VibeOS components from "Scaffold" to "Functional".

### 1. Daily Planner (VOS-015)
- **Web:** Build the full `Planner` component in `web/src/components/planner/`. 
- **Mobile:** Implement `mobile/app/(tabs)/plan.tsx`.
- **Logic:** Tasks must sync to the `tasks` table in Supabase. Ensure the UI respects the `is_archived` status.

### 2. Email Client (VOS-017)
- **Visuals:** Implement Inbox, Compose, and Thread views as per Figma.
- **Service:** Create `emailService.ts` to consume the `/api/v1/email` backend proxy.
- **Whitelist:** Ensure the UI clearly indicates if an address is "Whitelisted" or "Pending Audit".

### 3. Health Dashboard (VOS-018)
- **UI:** Implement a clean summary view of `health_metrics`.
- **AI Sync:** Display the `ai_analysis` field generated by the morning audit (VOS-010).

## Rules of Engagement
1. **GROUND TRUTH:** Mr. Blue MUST use the local UI assets in `docs/assets/ui_ground_truth/` as the primary reference for all component development (Web & Mobile). Figma is now a secondary reference.
2. **OLED First:** Every component must use the design tokens in `theme.ts` / `index.css`.
3. **Handoff:** You MUST provide a `HANDOFF.md` for each sub-task completion.
4. **No Code Leakage:** Keep your work restricted to the `feature/blue/` feature branches.

**The frontier is yours. Good luck.**

---
*Signed,*
**Mr. Pink**
