# AUDIT: VOS-049 — Cloud Run Egress & NAT Optimization

## 1. Objective
Verify that Mr. Red successfully optimized the vLLM Cloud Run infrastructure to eliminate excessive NAT data processing costs, specifically by caching the Qwen 9B model in the Docker image and restricting egress traffic.

## 2. Audit Findings

### 2.1. Codebase Verification (PASS)
- **`Dockerfile`**: Confirmed the addition of the `huggingface_hub` installation and the `snapshot_download` command to bake `Qwen/Qwen3.5-9B-Instruct` into the image. This permanently resolves the ~9GB cold start download issue.
- **`service.yaml`**: Confirmed `run.googleapis.com/vpc-access-egress` is correctly set to `private-ranges-only`. Public internet traffic (e.g., HuggingFace API, outgoing inference text) will now bypass the expensive NAT gateway.
- **Resources**: Confirmed `cpu: "4"`, `memory: "16Gi"`, and `timeoutSeconds: 300` across both `service.yaml` and `deploy.sh`.
- **Model Target**: Confirmed all files strictly target `Qwen/Qwen3.5-9B-Instruct`.

### 2.2. Infrastructure Note
- The handoff stated that Private Google Access (PGA) was enabled via CLI on the `default` subnet. As this is a pure GCP state change, it is noted as complete based on the handoff assertion, pending the next live test.

## 3. Verdict
**Status:** CEO APPROVED.
The codebase correctly implements the cost-saving measures outlined in the VOS-049 sendoff. The branch `feature/red/049-cost-optimization` is cleared for merge to `staging`.

---

> **NOTE — 2026-03-18 (Mr. Pink):** The model referenced in this audit (`Qwen/Qwen3.5-9B-Instruct`) has been superseded. The current cloud model is `Qwen/Qwen3.5-35B-A3B-GPTQ-Int4` as per `docs/MODEL_ARCHITECTURE_2026-03-18.md`. The cost-saving infrastructure patterns (model baking, egress restriction) documented here remain valid and carry forward.

*— Mr. Pink (Scout & Auditor)*
